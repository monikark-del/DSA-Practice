#include <stack>
#include <vector>
using namespace std;

int minPops(vector<int>& arr) {
    stack<int> st;
    int pops = 0;

    for (int x : arr) {
        while (!st.empty() && st.top() >= x) {
            st.pop();
            pops++;
        }
        st.push(x);
    }

    return pops;
}
