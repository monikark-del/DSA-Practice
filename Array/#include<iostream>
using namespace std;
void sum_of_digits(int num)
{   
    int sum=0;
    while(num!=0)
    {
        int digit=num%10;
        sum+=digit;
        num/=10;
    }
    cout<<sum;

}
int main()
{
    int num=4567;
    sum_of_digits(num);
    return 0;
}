too#include<iostream>
using namespace std;
void sum_of_digits(int num)
{   
    int sum=0;
    while(num!=0)
    {
        int digit=num%10;
        sum+=digit;
        num/=10;
    }
    cout<<sum;

}
int main()
{
    int num=4567;
    sum_of_digits(num);
    return 0;
}